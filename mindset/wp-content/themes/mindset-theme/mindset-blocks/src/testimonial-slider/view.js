/**
 * Swiper Initializer component.
 */
import { SwiperInit } from './swiper-init';

document.addEventListener( 'DOMContentLoaded', () => {
	const swiper = SwiperInit( '.swiper', swiper_settings );
} );